<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Taro" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_bckg</item>
        <item name="colorPrimaryDark">@color/bckg</item>
        <item name="colorAccent">@color/pinky</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Taro2" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_bckg</item>
        <item name="colorPrimaryDark">@color/bckg</item>
        <item name="colorAccent">@color/pinky</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/light_bckg</item>
    </style>

</resources>